# T1.6 — Upgrade vulnerable dependencies

`[R]` · size **S** · **Status: not started** · depends on T1.1

## Why

The pack's production audit at baseline `da87ab7` reported **1 high
(`react-router`) and 1 moderate (`hono`)**. Re-audit rather than trusting the
list — the tree has changed and advisories move.

## Do

1. Run the production audit with the manager chosen in T1.1 and record the exact
   current findings in this file before changing anything.
2. Upgrade to fixed versions. Prefer the smallest version bump that clears the
   advisory; do not take a major upgrade in this ticket without checking the
   changelog for breaking changes that affect routing or the Convex HTTP layer.
3. `react-router` is used with `BrowserRouter`. After upgrading, verify the routes
   in `src/main.tsx` still resolve and that the `/auth` → dashboard redirect works.
4. `hono` is pulled in for the Convex HTTP layer. After upgrading, verify the HTTP
   routes (`src/convex/http.ts`, the OAuth callbacks) still respond.
5. If a finding has no fix available, record it here with the advisory id, the
   reason it cannot be fixed yet, and the compensating control. Do not silence it
   by lowering the audit level.

## Acceptance

- The production audit reports no high findings.
- Every remaining moderate finding is listed in this file with a reason.
- Sign-in, the dashboard route and the OAuth callbacks were exercised after the
  upgrade (manually is acceptable for this ticket; automated coverage arrives in
  T1.5/T1.7).

## Proof to record in the PR

Before/after audit output, the version diff, and the manual checks performed.
