# MOSAI controller handoff

Updated 24 September 2026. Read `AGENTS.md`, `docs/MOSAI-IMPLEMENTATION-BLUEPRINT.md`, `docs/implementation/PROGRESS.md`, `docs/implementation/DEFERRED-WORK-BACKLOG.md`, and the relevant chapter reports before acting. This handoff and backlog are tracked on the local reconciliation branch; GitHub main and Freebuff do not have them yet.

## Product and evidence rules

MOSAI is a self-service marketing operating system: one authorized business context powers projects, personas, journeys and content across website/commerce, app building, CRM/automation, social, ads and growth. Each module must work alone and improve with connected data. Keep the experience simple for nontechnical customers without inventing success. `connected`, `published`, `live`, `sent`, `paid` and cancellation claims require verified provider evidence. Do not create worktrees; the owner chose existing checkouts. Use bounded Luna-low agents for independent work and controller review.

## Current repository checkpoint

- GitHub `julekpl/Mosai-friday` main was checked read-only on 24 September and remained `93a0df20ce5bf9568bcf5a2ebbed6080894a32f4`. Recheck it before any remote write.
- Existing `/Users/julianwitkowski/Downloads/mosai-friday/bp-02-codex` checkout holds clean local `codex/reconcile-reviewed` at **`71b1ebe`**, 16 commits ahead of old main. It combines reviewed BP-02/03, BP-05, BP-06 admin/S1, and partial BP-07/09/10/11/12/19. BP-05 source is preserved as `0fde75c` on `codex/bp-05` in the existing `bp-05-codex` checkout. No integrated commit has been pushed or CI-verified.
- On integrated commit `7e880f6`, **436/436 units, typecheck, lint (0 errors/28 existing warnings), public-function audit, capability audit, all 66 data-registry entries, and build passed**. BP-09 `aiRuns` is now registered for both project deletion and user-only account cleanup; the old immediate-deletion test was updated for the 30-day durable job. Commit `71b1ebe` changed only the ads test's synthetic environment setup; its focused 3/3 tests pass. On `71b1ebe`, local browser E2E passed **20**, with the live OTP test skipped. The inert browser backend was `http://127.0.0.1:9999`.
- `bun run check` still exits 1 at the active secret scan because tracked `.env.keys` is present; the matched value was redacted. A newly detected synthetic ads-test pattern was fixed in `71b1ebe`, leaving only the pre-existing finding. This is **not a green release gate**. `check:codegen` needs an approved development `CONVEX_DEPLOYMENT`; no production backend or provider was contacted.
- BP-05's independent Luna review found the prior multi-org, shared-membership, cursor, late-subscription and export-access defects fixed, and independently passed 39 focused lifecycle/registry/deletion tests. Full source checkout passed 375 units. The integrated version has local combined tests but still needs real test-provider/deployment proof, exact-SHA CI, and a customer-facing export/download UI. Account deletion uses a 30-day grace; active/stale subscriptions block finalization. Do not delete live data.
- The owner selected the **Stripe test catalog** as current price source and **cancellation at period end**; real Stripe catalog/portal/webhook behavior has not been verified. The owner selected **EU/EEA, multilingual first** with launch languages in order: English, German, French, Italian, Spanish, Polish, Dutch, Romanian, Greek, Hungarian, Czech, Swedish, Portuguese, Bulgarian, Danish. The app has no shared localization system or translated user journeys yet.

## Existing checkouts and task boundaries

- `bp-05-codex`: clean `codex/bp-05` at `0fde75c`; source task [BP-05 Codex task](codex://threads/01a0cf38-322a-78b0-8e69-e3d50d984537) is idle. Its report is `docs/implementation/reports/BP-05-export-deletion-lifecycle.md` on both source and reconciliation branches.
- `bp-03-codex`: BP-07/S1 preserved at `db64359` on `codex/bp-07-s1-receipt`; its branch was clean as of this update. A Luna-low BP-12 locale-foundation agent has been asked to use this **existing** checkout on a separate local branch based on `71b1ebe`, without new worktrees or schema edits. Check agent state before changing that checkout.
- `review-current`: BP-19 social setup gate preserved at `eb39e27` on `codex/bp-19-social-action-connection-gate`, plus three untracked controller documents that must not be discarded. BP-06/S1 is preserved at `5c8aba6` on `codex/bp-06-admin-stats`. The tracked handoff/backlog in `bp-02-codex` supersede stale untracked copies.
- BP-02 auth `9a12f03`, BP-03 compatibility `c1006d3`, BP-10 source status `c9da779`, BP-11 journey origin `535ae7e`, BP-11 onboarding `7b5ca43`, BP-12 manual-insight truth `e57f56e`, BP-09/S1 gateway `d21fa7c`, BP-06 admin `016c087`, BP-19 social gate `eb39e27`, and BP-07/S1 ads claim `db64359` remain individually preserved. Their reports and `PROGRESS.md` define exact partial status. No whole BP package is proven complete.

## Freebuff, blocked release and next integration

The existing [Freebuff project chat](https://freebuff.com/web/project/old-paths-carry?view=versions) is paused. Vly blocks Git/GitHub commands. Workspace History showed checkpoint `93a0df2`; six selected-file SHA-256 values matched old main, which is **not** full-tree parity. The owner chose Codex to reconcile and review changes first, then give Freebuff an exact GitHub commit and recheck its checkpoint/files before it resumes. Do not tell Freebuff that local `71b1ebe` is available there; it is not pushed.

T0.1/B3 secret remediation remains owner-run: rotate/revoke affected credential, remove tracked secret material, follow `docs/runbooks/secret-rotation.md`, then prove both working-tree and full-history scans pass on the exact default-branch commit. Never print values, weaken rules, call CI green, deploy or operate on production to bypass this. BP-03 legacy verified rows also require inventory before a public publishing rollout. The missing reference documents in `PROGRESS.md` B1 still block some architecture/evaluation claims.

Next controller steps: review the BP-12 locale-foundation slice if delivered; rerun affected checks after integration; update `PROGRESS.md` and backlog statuses with exact SHA; check remote main again; prepare a reviewable remote synchronization plan. Keep Freebuff paused until an authorized GitHub commit is available, and keep the release gate red until secret remediation and exact-SHA CI pass. For all remaining packages, follow the blueprint and do not mark a complete chapter from a narrow UI or mocked slice.

## Copy-ready prompt for the next controller task

```text
Continue as MOSAI controller. Read /Users/julianwitkowski/Downloads/mosai-friday/bp-02-codex/docs/implementation/CONTINUITY-HANDOFF.md, its linked backlog, AGENTS.md, blueprint, PROGRESS.md and chapter reports. Existing integration checkout bp-02-codex has local clean branch codex/reconcile-reviewed at 71b1ebe, combining BP-02/03/05/06 and partial BP-07/09/10/11/12/19. GitHub main last checked at 93a0df2; recheck before any remote action. No new worktrees. BP-05 source is preserved at 0fde75c in bp-05-codex; integrated 436 units/typecheck/lint/audits/build passed on 7e880f6, targeted ads test and local browser E2E 20 pass/1 live OTP skip on 71b1ebe. The tracked .env.keys still fails the secret scan and release gate; codegen lacks a dev deployment, and no real provider proof or exact-SHA CI exists. Owner chose Stripe test catalog, period-end cancellation, and EU/EEA multilingual-first with 15 languages listed in backlog D-13. A Luna-low locale-foundation agent may be using the existing bp-03-codex checkout; check its status. Review each slice against product vision and exact acceptance, keep partial statuses truthful, preserve all local work, and give Freebuff https://freebuff.com/web/project/old-paths-carry?view=versions only an authorized, reviewed GitHub SHA after remote synchronization. Do not print secrets, weaken gates, deploy or make external writes.
```
