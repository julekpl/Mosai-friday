# MOSAI controller handoff

Updated 24 September 2026. Read `AGENTS.md`, `docs/MOSAI-IMPLEMENTATION-BLUEPRINT.md`, `docs/implementation/PROGRESS.md`, `docs/implementation/DEFERRED-WORK-BACKLOG.md`, and relevant chapter reports. The blueprint defines full acceptance; a locally passing slice is not a completed package or release.

## Product and truth rules

MOSAI is one authorized business context powering projects, personas, journeys and content across website/commerce, app building, CRM/automation, social, ads and growth. Each module must work alone and improve with connected data. Nontechnical customers need clear next actions without invented success. `connected`, `published`, `live`, `sent`, `paid` and cancellation claims require verified provider evidence. Keep the secret-scan/release gate red until owner remediation is proven. No new worktrees are authorized; use only existing checkouts. Low-usage agents do bounded independent work, and the controller reviews their diffs and tests.

## Last verified development sync

- GitHub main now contains BP-17/S1a contact import and truthful consent UI at **`a594cf75175287f63967264b2b5852cb4e3d792b`**, followed by documentation sync `5f73b133f37d1322220cd67f957e65f843cda6b4`. Exact [CI 35977893382](https://github.com/julekpl/Mosai-friday/actions/runs/35977893382) and [CI 35978781895](https://github.com/julekpl/Mosai-friday/actions/runs/35978781895) passed frozen install, Convex codegen drift, typecheck, lint, unit, Playwright E2E and axe accessibility; **both secret-scan jobs failed** in each. BP-17/S1 remains in progress: no verified opt-in ledger, sending, suppression, legacy-email migration or authenticated import browser journey. Freebuff independently matched four sampled CRM code/test files at `a594cf7`; Workspace History includes `a594cf7` and `5f73b13` with Vly merge entries. This is sampled content parity, never full-tree proof.
- The earlier main sync **`e6f3f104862565c1632cd13c563e23cdcc6f8616`** and documentation follow-up `b57c1dc` also passed non-security CI and failed both secret scans. `bun run check` still fails locally at the known tracked `.env.keys:8` finding. Never call the release gate green.
- The [existing Freebuff project](https://freebuff.com/web/project/old-paths-carry?view=versions) is paused for coding. Vly blocks Git/GitHub commands. Freebuff previously matched seven sampled non-secret files to `e6f3f10`, five documents to `b57c1dc`, and four CRM files to `a594cf7`; its History shows Vly WIP/merge entries. These checks never establish a full-tree match. Do not resume the old BP-09/S1 chat for coding. Each future Freebuff chapter needs a new chat with a fresh exact checkpoint and a bounded file scope. Recheck GitHub main, CI and Freebuff before any remote write or new assignment.
- No production deployment, live provider write, PR or new worktree was made for these slices. The main push was development synchronization only.

## Current integration work and checks

- Uncommitted BP-13/S1 public-projection interface work remains isolated in `bp-02-codex`: `src/convex/modules/buildWebsite/publicProjection.ts`, `src/convex/schema.ts`, `src/convex/lib/capabilities.ts`, `tests/unit/publish-truth.test.ts`, and `reports/BP-13-S1-public-projection.md`. It resolves a synthetic verified-host receipt to pinned release content and passes focused tests, but no hostname verifier, separate-origin HTML renderer, domain/TLS proof or actual public site exists. **Do not sync it yet:** the new Convex module requires official generated bindings, and this checkout has no `CONVEX_DEPLOYMENT`; `convex codegen --dry-run` exits 1. Do not hand-edit `_generated` or mark BP-13/S1 complete. O4 public hosting/domain decision is pending.
- The BP-17/S1a slice changed only contacts, CSV parser, Customers UI and tests, plus scoped progress/backlog/report documents. Combined local verification in the shared checkout passed **473/473** units, typecheck, lint (28 existing warnings), build, three audits and hermetic E2E **20 passed/1 live OTP skipped**. `bun run check` still fails at the pre-existing secret scan. Exact main-commit CI is above; its E2E does not prove an authenticated CRM import.
- Existing checkout `/Users/julianwitkowski/Downloads/mosai-friday/bp-02-codex` is on local branch `codex/bp19-next-actions`, with the reviewed BP-19 Overview next-action/checklist and BP-09/S2 social draft-variant ContextPack follow-up committed at `e6f3f10` and synced to main. The latter was copied after controller review from the preserved `bp-05-codex` source checkout. Do not discard or overwrite either checkout. `bp-03-codex` preserves uncommitted BP-08/S1 source; `review-current` preserves pre-existing untracked coordination documents.
- The social follow-up rejects foreign persona, campaign and content references before model calls, rechecks cross-project references at draft insertion, labels caller text untrusted, and rejects unrequested model-output platforms. The platform test was red before the fix: a LinkedIn-only request created an extra TikTok draft. It is now green. The Overview guide uses saved-row and entitlement state, calls rows **saved**, not reviewed/complete, and routes to real modules or plan options. No durable persona/journey review signal exists yet; the guide cannot certify source quality.
- Combined local verification after integration: **464/464 unit tests in 34 files**, typecheck pass, lint exit 0 with 28 existing warnings, build pass, three authorization/capability/data-registry audits pass (66 tables), `git diff --check` pass. An initial unit invocation without Bun on `PATH` failed six spawned audit-gate cases (`spawnSync` status null); the correctly configured rerun passed all 464. The hermetic Playwright suite passed **20/20 with one intentional live-OTP skip** using CI's inert `VITE_CONVEX_URL=https://e2e-test-only.convex.cloud`; it includes public/auth axe checks but not authenticated Overview/mobile/social behavior. First Playwright attempt could not bind localhost under the sandbox (EPERM); an authorized local run completed. `bun run check` remains exit 1 at the existing secret finding; the three audits were run separately and passed. No dev Convex deployment or real provider was used.
- Reports: `reports/BP-09-S2-social-copilot.md`, `reports/BP-19-next-action.md` and the earlier BP-08/S1, BP-09/S2 and BP-19/mobile reports. Six captured public-preview screenshots and findings are under `docs/implementation/ux-audit-2026-09-24/`. The preview demonstrates a fictional walkthrough and email-code entry, not a real completed onboarding journey.

## Preserved decisions and blockers

- Owner chose the **Stripe test catalog** as current package price source and **subscription cancellation at period end**. Real catalog/checkout/portal/webhook proof remains open. BP-05 deletion retains a 30-day grace and blocks on active or stale paid subscriptions; no live deletion is authorized. Shared organizations must preserve other active members.
- Owner chose **EU/EEA, English-only first release**. German, French, Italian, Spanish, Polish, Dutch, Romanian, Greek, Hungarian, Czech, Swedish, Portuguese, Bulgarian and Danish are later phases only when requested. BP-12 has a type-only measurement/consent contract and no public optional tracking collector until server-owned consent and approved English EU/EEA policy exist.
- Exposed-secret remediation remains owner-run: rotate/revoke affected credentials, remove tracked secret material, follow `docs/runbooks/secret-rotation.md`, and prove both scans on the exact default-branch commit. Never print values, weaken scan rules, allow-list findings, deploy or claim release readiness. BP-03 legacy verified rows need inventory before public publishing rollout. Missing pack references in `PROGRESS.md` B1 block some architecture/evaluation claims; do not invent them.
- BP-08 organization-bound OAuth state, token policy, account selection/sync/provider proof; BP-09 Build chat/ads copilot/scheduling context, claim grounding/evals/run reconciliation; BP-06 add-ons/admin commerce; BP-10/11 reviewed research and journeys; BP-13–18 real customer-facing modules; BP-19 observed usability; and BP-20 release proof remain open. The last two local slices do not complete these chapters.

## Next controller actions

1. Review the current diff and new reports. Before development sync, run `git rev-parse HEAD`, `git status`, relevant tests and security checks; do not infer exact-SHA CI from local checks. After any authorized push, record CI outcomes and recheck Freebuff History plus representative file hashes.
2. Continue independent chapters only in existing checkouts without overwriting preserved work. Keep Freebuff coding paused until it has a fresh verified checkpoint and a new chapter chat. Keep release status blocked even when non-security jobs pass.
3. Prioritize real customer journeys: accurate source review → persona/journey/content approval, organization billing and add-ons, provider account selection/sync, deployable public site and commerce, app builder, ads, CRM, social operations and evidence-backed growth. Ask the owner for decisions only when an external, commercial or irreversible step genuinely depends on them.

## Copy-ready controller prompt

Continue as MOSAI controller. Read `AGENTS.md` and `/Users/julianwitkowski/Downloads/mosai-friday/bp-02-codex/docs/implementation/CONTINUITY-HANDOFF.md`, plus linked blueprint, PROGRESS.md, backlog and reports. Inspect current git status/HEAD and GitHub main/CI before acting. The latest reviewed code sync is `a594cf75175287f63967264b2b5852cb4e3d792b` (BP-17/S1a contact import and truthful consent UI), followed by docs `5f73b13`; both exact CI runs passed install/codegen/typecheck/lint/unit/E2E/a11y and FAILED both secret scans. Freebuff matched four sampled CRM files at `a594cf7`, and History includes both commits with Vly merges; never infer full-tree parity. The uncommitted BP-13/S1 resolver in bp-02-codex is local only and cannot be synced until official Convex codegen runs against a configured dev deployment; it has no real domain verifier/public HTML. Combined local verification passed 473 units, typecheck, lint (28 existing warnings), build, three audits and hermetic Playwright 20 passed/1 live OTP skipped. `bun run check` still fails on tracked `.env.keys`; release is BLOCKED. Preserve the uncommitted source checkouts, no new worktrees, no production/provider writes, no secret disclosure. Owner chose Stripe test catalog, period-end cancellation and EU/EEA English-only first release. Review and sync only verified development work under applicable authorization, then delegate bounded remaining chapters without collisions or fake success.
