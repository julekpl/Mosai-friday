# MOSAI controller handoff

Updated 24 September 2026. Read `AGENTS.md`, `docs/MOSAI-IMPLEMENTATION-BLUEPRINT.md`, `docs/implementation/PROGRESS.md`, `docs/implementation/DEFERRED-WORK-BACKLOG.md`, and the relevant chapter reports before acting. The blueprint defines scope and acceptance; a locally passing slice is not a finished package or a release.

## Product and evidence rules

MOSAI is a self-service marketing operating system: one authorized business context powers projects, personas, journeys and content across website/commerce, app building, CRM/automation, social, ads and growth. Each module must work alone and improve with connected data. Keep the experience simple for nontechnical customers without inventing success. `connected`, `published`, `live`, `sent`, `paid` and cancellation claims require verified provider evidence. Do not create new worktrees; the owner chose existing checkouts. Keep the secret-scan/release gate red until owner remediation is proven. The controller uses bounded low-usage agents for independent work and reviews their diffs and tests.

## GitHub and Freebuff checkpoint

- GitHub `julekpl/Mosai-friday` main was verified at **`274877fdbdaedda846156242779db5452a7aada6`** after the owner authorized a development-only sync. Exact [CI run 35965337725](https://github.com/julekpl/Mosai-friday/actions/runs/35965337725) completed **failure**: install, typecheck, lint, unit, Convex codegen drift, Playwright E2E and axe accessibility jobs passed; both security jobs failed at the known tracked `.env.keys` / historical-secret scan. This is not a green release gate. Recheck main and its CI before any new remote write.
- The existing [Freebuff project](https://freebuff.com/web/project/old-paths-carry?view=versions) is paused. Vly blocks Git/GitHub commands. Workspace History showed Latest `274877f`; sampled non-secret file hashes matched the development commit. This is sampled parity, not full-tree proof. Freebuff has not received the new local slices. Start each future Freebuff chapter in a **new chat** with an exact current checkpoint, bounded scope, and explicit branch/main workflow; do not resume its old BP-09/S1 chat or imply provider proof.
- No production deployment, live provider write, PR or new worktree was made for the current local slices. The previous main push was for development synchronization only and did not clear the release gate.

## Current local integration and evidence

- Existing checkout `/Users/julianwitkowski/Downloads/mosai-friday/bp-02-codex` is now on local branch `codex/bp19-mobile-navigation` based on `origin/main` `274877f`. It has **uncommitted, locally integrated** BP-19 mobile navigation, BP-08/S1 OAuth state/origin, and BP-09/S2 ContextPack changes plus their reports. The prior `codex/reconcile-reviewed` branch remains preserved. Do not discard, stash or overwrite this working tree.
- Independent source checkouts remain preserved: `bp-03-codex` branch `codex/bp-08-connections` contains the uncommitted BP-08/S1 source; `bp-05-codex` branch `codex/bp-09-s2-context-pack` contains the uncommitted BP-09/S2 source. Their earlier BP-12 and BP-07/BP-05 branches remain in Git history. `review-current` still has pre-existing untracked controller documents; leave them untouched.
- Controller independently applied the disjoint diffs and reconciled the single shared `src/convex/lib/capabilities.ts` helper registrations. On the **combined uncommitted checkout**, `bun run test:unit` passed **456/456 in 33 files**, typecheck passed, lint exited 0 with **28 pre-existing warnings**, public-function/capability/data-registry audits passed (66 registered tables), build passed, and `git diff --check` passed. `bun run check` exits **1** at the known secret scan. Do not call it green.
- The hermetic browser suite passed **20/20 with one intentional live-OTP skip** using CI's synthetic `VITE_CONVEX_URL=https://e2e-test-only.convex.cloud`. A first local attempt without that variable rendered blank and was stopped after failures; a browser diagnostic identified missing Convex URL. The correctly configured rerun passed. This suite does **not** reach an authenticated mobile drawer, real provider, or live OTP inbox. Local `check:codegen` was not rerun because an approved dev `CONVEX_DEPLOYMENT` is absent; the previous exact-commit CI codegen job passed on `274877f`.
- New chapter reports: `reports/BP-08-S1-oauth-state-origin.md`, `reports/BP-09-S2-context-pack.md`, `reports/BP-19-mobile-navigation.md`. All three are **implemented_unverified** slices. BP-08 full account selection/sync remains open; BP-09 build chat/ads/social ContextPack coverage and S3 evaluations remain open; BP-19 guided journeys and observed users remain open. See backlog D-14, D-17 and D-19.

## Preserved decisions and blocking work

- Owner selected **Stripe test catalog** as current package price source and **subscription cancellation at period end**. Real Stripe catalog/portal/webhook journeys remain unverified. BP-05 deletion keeps a 30-day grace and blocks on active or stale paid subscriptions; no live data deletion is authorized. Shared organizations must preserve other active members.
- Owner selected **EU/EEA, English-only first release**. German, French, Italian, Spanish, Polish, Dutch, Romanian, Greek, Hungarian, Czech, Swedish, Portuguese, Bulgarian and Danish are later phases only when requested. BP-12 has a type-only measurement/consent contract and **no public optional tracking collector** until a server-owned consent record and approved English EU/EEA policy exist.
- The exposed-secret remediation remains owner-run: rotate/revoke affected credentials, remove tracked secret material, follow `docs/runbooks/secret-rotation.md`, and prove both working-tree and full-history scans on the exact default-branch commit. Never print values, weaken scan rules, allow-list the finding, deploy, or claim release readiness. BP-03 legacy verified rows need inventory before a public publishing rollout. Missing pack references in `PROGRESS.md` B1 block some architecture/evaluation claims; do not invent them.
- BP-07's atomic ads/deletion fence is already integrated on main, but ambiguous provider operations still need operator reconciliation and real provider proof. BP-05 needs customer export/download UI and dev-provider proof. The original product vision also requires BP-06/S2–S3 billing/add-ons/admin, BP-10/11 source evidence/journeys, BP-13–18 operational website/commerce/app/ads/CRM/social modules, BP-19 observed usability, and BP-20 release proof. None is fully proven by the three local slices.

## Next controller steps

1. Review the combined uncommitted diff and chapter reports, then decide the next development sync to main under the applicable authorization. After any commit/push, obtain exact-SHA CI and recheck Freebuff History plus sampled files. Keep release status blocked even if non-security jobs pass.
2. Give Freebuff a new chapter only after its checkpoint is verified. Codex can continue bounded non-overlapping work in existing checkouts; preserve all uncommitted source branches and coordination files. Do not let Freebuff autosave over a parallel Codex change.
3. Finish the highest-risk gaps without false product claims: BP-08 real account selection/sync and token policy, BP-09 remaining call sites/evaluations, BP-06 organization billing/add-ons, BP-19 guided next actions and authenticated mobile/browser observation, then the blueprint's remaining modules and journeys. Ask the owner only for decisions that genuinely block a dependent external or commercial action.

## Copy-ready prompt for a new controller task

```text
Continue as MOSAI controller. Read AGENTS.md and /Users/julianwitkowski/Downloads/mosai-friday/bp-02-codex/docs/implementation/CONTINUITY-HANDOFF.md, plus its linked blueprint, PROGRESS.md, deferred backlog and chapter reports. Recheck GitHub main and Freebuff History before any remote action. GitHub main was 274877f (24 Sep 2026), with CI run 35965337725 passing non-security jobs but FAILING both secret-scan jobs. Freebuff was sampled at 274877f and its old BP-09 chat is paused. The existing bp-02-codex checkout has uncommitted, locally integrated BP-08/S1 OAuth state/origin, BP-09/S2 ContextPack, and BP-19 mobile navigation on branch codex/bp19-mobile-navigation. Source diffs remain uncommitted in existing bp-03-codex and bp-05-codex checkouts; preserve them and the review-current untracked docs. Combined local checks passed 456 units, typecheck, lint with 28 existing warnings, audits, build, and synthetic browser E2E 20 passed/1 live OTP skipped. bun run check still exits 1 at tracked .env.keys; release remains BLOCKED. No new worktrees, no production/provider writes or secret disclosure. Owner chose Stripe test catalog, period-end cancellation, and EU/EEA English-only first release. Review the combined diff, keep exact package statuses honest, sync reviewed development changes only under applicable authorization, verify CI and Freebuff checkpoint, then coordinate bounded remaining chapters without collisions or fake success.
```
