# MOSAI controller handoff

Updated 24 September 2026. Read `AGENTS.md`, `docs/MOSAI-IMPLEMENTATION-BLUEPRINT.md`, `docs/implementation/PROGRESS.md`, `docs/implementation/DEFERRED-WORK-BACKLOG.md`, and the relevant chapter reports before acting. The reviewed development integration reached GitHub main at `838aa128d2942ad1477b31ad0f50a4352b9c32d4`. This handoff includes a later documentation-only sync note; check the latest commit before acting.

## Product and evidence rules

MOSAI is a self-service marketing operating system: one authorized business context powers projects, personas, journeys and content across website/commerce, app building, CRM/automation, social, ads and growth. Each module must work alone and improve with connected data. Keep the experience simple for nontechnical customers without inventing success. `connected`, `published`, `live`, `sent`, `paid` and cancellation claims require verified provider evidence. Do not create worktrees; the owner chose existing checkouts. Use bounded Luna-low agents for independent work and controller review.

## Current repository checkpoint

- GitHub `julekpl/Mosai-friday` main was verified at `838aa128d2942ad1477b31ad0f50a4352b9c32d4` after the owner explicitly authorized a development-only push despite the known secret-scan failure. Recheck it before another remote write.
- Existing `/Users/julianwitkowski/Downloads/mosai-friday/bp-02-codex` checkout holds `codex/reconcile-reviewed`; the checked code commit is **`914346d`** and the reviewed development sync was **`838aa12`** (later documentation edits may follow). Run `git rev-parse HEAD` for the current tip. It combines reviewed BP-02/03/05/06 and partial BP-07/09/10/11/12/19. BP-05 source `0fde75c`, BP-12 contract source `de219e0`, and BP-07/BP-05 fence source `3e9ce53` remain in existing checkouts.
- On integrated commit `914346d`, **442/442 units, typecheck, lint (0 errors/28 existing warnings), public-function audit, capability audit, all 66 data-registry entries, and build passed**. The first combined run failed one capability audit test because BP-12 placed a disabled public collector in an internal-only file; `914346d` removed it and the affected checks were rerun green. BP-09 `aiRuns` is registered for project deletion and user-only account cleanup. BP-07/BP-05's new interleaving tests cover request/claim ordering and success/error receipt completion. On earlier `71b1ebe`, local browser E2E passed **20**, with live OTP skipped; its inert backend was `http://127.0.0.1:9999`. E2E has not been rerun on `914346d` because these later changes have no browser UI.
- `bun run check` still exits 1 at the active secret scan because tracked `.env.keys` is present; the matched value was redacted. A newly detected synthetic ads-test pattern was fixed in `71b1ebe`, leaving only the pre-existing local finding. Exact-commit [CI run 35963489882](https://github.com/julekpl/Mosai-friday/actions/runs/35963489882) on `838aa12` completed **failure**: install, typecheck, lint, unit, codegen drift, e2e and a11y jobs passed; working-tree and full-history secret-scan steps failed. This is **not a green release gate**. Local `check:codegen` still needs an approved development `CONVEX_DEPLOYMENT`; no production backend or provider was contacted.
- BP-05's independent Luna review found the prior multi-org, shared-membership, cursor, late-subscription and export-access defects fixed, and independently passed 39 focused lifecycle/registry/deletion tests. Full source checkout passed 375 units. The integrated version has local combined tests and exact-SHA CI evidence, but still needs real test-provider/deployment proof, a customer-facing export/download UI and a green release gate. Account deletion uses a 30-day grace; active/stale subscriptions block finalization. Do not delete live data.
- A later independent cross-chapter review found a **high-severity BP-07/BP-05 in-flight ads/deletion race**: deletion could erase an executing change before its external receipt was stored. The atomic fence is now integrated as `7b120dc`, with red-first interleaving regressions and an explicit blocked reason. An ambiguous claim remains blocked for manual reconciliation; no live-provider recovery proof exists. Backlog D-18 retains operational recovery and a separate lower-severity AI-run telemetry path.
- The owner selected the **Stripe test catalog** as current price source and **cancellation at period end**; real Stripe catalog/portal/webhook behavior has not been verified. The owner selected **EU/EEA, English-only first release** on 24 September. The other 14 requested languages are deferred until explicitly requested: German, French, Italian, Spanish, Polish, Dutch, Romanian, Greek, Hungarian, Czech, Swedish, Portuguese, Bulgarian, Danish. The app has no shared localization system or translated journeys; do not claim multilingual launch readiness.

## Existing checkouts and task boundaries

- `bp-05-codex`: clean `codex/bp-07-bp05-deletion-fence` at `3e9ce53`; original `codex/bp-05` remains preserved at `0fde75c`. The source task [BP-05 Codex task](codex://threads/01a0cf38-322a-78b0-8e69-e3d50d984537) is separate; reports are `BP-05-export-deletion-lifecycle.md` and `BP-07-BP-05-deletion-fence.md`.
- `bp-03-codex`: clean `codex/bp-12-s1-consent-contract` at `de219e0`; original `codex/bp-07-s1-receipt` remains preserved at `db64359`. The first-release locale-foundation work was stopped before edits when the owner chose English-only.
- `review-current`: BP-19 social setup gate preserved at `eb39e27` on `codex/bp-19-social-action-connection-gate`, plus three untracked controller documents that must not be discarded. BP-06/S1 is preserved at `5c8aba6` on `codex/bp-06-admin-stats`. The tracked handoff/backlog in `bp-02-codex` supersede stale untracked copies.
- BP-02 auth `9a12f03`, BP-03 compatibility `c1006d3`, BP-10 source status `c9da779`, BP-11 journey origin `535ae7e`, BP-11 onboarding `7b5ca43`, BP-12 manual-insight truth `e57f56e`, BP-09/S1 gateway `d21fa7c`, BP-06 admin `016c087`, BP-19 social gate `eb39e27`, and BP-07/S1 ads claim `db64359` remain individually preserved. Their reports and `PROGRESS.md` define exact partial status. No whole BP package is proven complete.

## Freebuff, blocked release and next integration

The existing [Freebuff project chat](https://freebuff.com/web/project/old-paths-carry?view=versions) is paused. Vly blocks Git/GitHub commands. After the development sync, Freebuff Workspace > History showed **Latest `838aa12`**, and its read-only agent verified SHA-256 parity for `AGENTS.md`, the blueprint, `PROGRESS.md`, `schema.ts`, `capabilities.ts` and `dataRegistry.ts` against that GitHub commit. This is sampled file parity, **not proof of the entire tracked tree**. Freebuff made no edits, restore, autosave or publish during verification. BP-09/S1 is already implemented by Codex; do not ask Freebuff to redo it. Start any future Freebuff chapter in a new chat against a verified current checkpoint, with scope and main-branch authorization settled for that chapter.

T0.1/B3 secret remediation remains owner-run: rotate/revoke affected credential, remove tracked secret material, follow `docs/runbooks/secret-rotation.md`, then prove both working-tree and full-history scans pass on the exact default-branch commit. Never print values, weaken rules, call CI green, deploy or operate on production to bypass this. BP-03 legacy verified rows also require inventory before a public publishing rollout. The missing reference documents in `PROGRESS.md` B1 still block some architecture/evaluation claims.

Next controller steps: check the latest GitHub/Freebuff checkpoint after this documentation-only update, then assign a bounded new chapter on a non-overlapping scope. BP-12 collection stays absent until an owner-approved English EU/EEA consent policy and server-owned consent proof exist. BP-07 needs an operator path for ambiguous claims. Keep the release gate red until secret remediation and both secret scans pass on an exact commit. For all remaining packages, follow the blueprint and do not mark a complete chapter from a narrow UI or mocked slice.

## Copy-ready prompt for the next controller task

```text
Continue as MOSAI controller. Read /Users/julianwitkowski/Downloads/mosai-friday/bp-02-codex/docs/implementation/CONTINUITY-HANDOFF.md, its linked backlog, AGENTS.md, blueprint, PROGRESS.md and chapter reports. Existing integration checkout bp-02-codex has codex/reconcile-reviewed; code checkpoint 914346d and development-sync commit 838aa12 combined BP-02/03/05/06 and partial BP-07/09/10/11/12/19. Run git rev-parse HEAD and recheck GitHub main before another remote write; this handoff has a later documentation-only update. No new worktrees. BP-05 source 0fde75c, BP-12 contract source de219e0 and BP-07/BP-05 fence source 3e9ce53 are preserved. The combined branch passed 442 units, typecheck, lint with 28 existing warnings, audits and build; earlier local browser E2E passed 20 with 1 live OTP skip. Exact-commit CI run 35963489882 for 838aa12 passed install/typecheck/lint/unit/codegen/e2e/a11y but FAILED both secret-scan jobs. Freebuff History showed Latest 838aa12 and six sampled file hashes matched; its BP-09 chat remains paused and cannot prove full-tree parity. BP-12 has only a type contract and no public analytics collector until server-owned consent and approved English EU/EEA policy exist. BP-07 ambiguous execution recovery remains open. The tracked .env.keys still blocks release, and no real provider proof exists. Owner chose Stripe test catalog, period-end cancellation, and EU/EEA English-only first release; 14 other requested languages are deferred. Select a bounded next chapter, keep each status truthful, preserve all local work, and confirm its exact main-branch workflow before Freebuff edits. Do not print secrets, weaken gates, deploy or make external writes.
```
