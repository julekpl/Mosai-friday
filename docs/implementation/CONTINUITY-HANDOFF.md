# MOSAI controller handoff

Updated 24 September 2026. Recheck Git and active tasks before acting. This local coordination file and `DEFERRED-WORK-BACKLOG.md` are untracked, so neither GitHub nor Freebuff has them.

## Product and evidence rules

MOSAI should let a small business sign in, build one authorized project/persona/content context, and use it seamlessly across website/commerce, app, CRM, social, ads and growth. Each module must work alone and improve with connected data. External `connected`, `published`, `live`, `sent`, `paid` and cancellation claims need verified provider evidence. Read `AGENTS.md`, `docs/MOSAI-IMPLEMENTATION-BLUEPRINT.md`, `docs/implementation/PROGRESS.md`, `docs/pack/10-build-backlog.md`, module contracts and chapter reports. No new worktrees: the owner explicitly chose existing checkouts. Use focused Luna-low agents for bounded tasks, with controller review.

## Exact repository state

- GitHub `julekpl/Mosai-friday` main was fetched on 23 September: `93a0df20ce5bf9568bcf5a2ebbed6080894a32f4` (tracked tree `99fc57599cb6a475f55e24d1c088c8e463115716`). Re-fetch before integration.
- BP-03 compatibility fix is preserved as **local commit `c1006d3` on `codex/bp-03-final`**. BP-03's 21 focused tests passed under the controller. It is not merged to GitHub main: verified legacy release rows without frozen route/title snapshots need inventory and rollout planning. Its report is in the BP-03 commit at `docs/implementation/reports/BP-03-publish-truth.md`. Task: `codex://threads/01a0cefc-fc65-7eb3-9d1a-0e17a4c0d231`.
- BP-10 source-status slice is preserved as **local commit `c9da779` on `codex/bp-10-research-status`**. The existing `review-current` checkout was then reused for `codex/bp-06-admin-stats` at `origin/main`; its bounded admin-statistics slice is now **local commit `016c087`**. BP-10 and BP-06 reports are in their respective commits under `docs/implementation/reports/`. Controller verified BP-10's 336/336 units and BP-06's 335/335 units; typecheck in this checkout is blocked by an incomplete local dependency install missing declared `three` in untouched `RelationMap.tsx`. Both chapters remain partial: BP-10 source statuses are not persisted, and BP-06 customer billing remains unverified.
- BP-05 remains uncommitted in existing `/Users/julianwitkowski/Downloads/mosai-friday/bp-05-codex`, branch `codex/bp-05` from `93a0df2`. Task `codex://threads/01a0cf38-322a-78b0-8e69-e3d50d984537` is active on another focused follow-up. Its bounded resumable deletion/export revision independently passed 361/361 units, typecheck and data-registry audit. A fresh independent review found untested release blockers: account export uses a project ID as a pagination cursor after the first project; the final parent's child rows are skipped; inactive organization membership can retain project export access; a subscription arriving after the billing cleanup cursor passes can be deleted with its organization; subscription obligation reads grow with row count; export scheduling can starve running jobs. The task has the precise cases and must add failing regressions before fixes. BP-05 is not integrated. No live data was deleted.
- BP-02 auth work started from older base `be1ade5a6832418a455d7086037c18c34130c245`. A Luna review fixed one stale live-OTP assertion; owner email provider/domain and privileged step-up decisions remain open.
- BP-02 auth work was preserved as **local commit `9a12f03` on `codex/bp-02-auth`**. The existing `bp-02-codex` checkout holds local reconciliation branch `codex/reconcile-reviewed` from `origin/main`. It combines BP-02/03, BP-06 admin/S1, partial BP-07/09/10/11/12/19 slices (tip **`1c65224`**), preserving chapter entries. Controller verified **394/394 unit tests, typecheck and both authorization audits** on this tip. The preceding `1203d62` tree passed 385 units, typecheck, lint 0 errors/28 existing warnings, both audits, build and browser E2E 20 passed/1 opt-in live OTP skipped (hermetic inert backend URL); the BP-11 onboarding UI tip before the BP-07/19 cherry-picks also passed build. Full lint/build/browser checks on `1c65224` remain to rerun after BP-05 reconciliation. The previous exact `bun run check` on `5de0c48` exited 1 at the known secret scan; `.env.keys` remains tracked, so the release gate is still blocked. No integrated commit is pushed, merged to GitHub main, CI-verified, or shared with Freebuff. BP-05 is not in it. `check:codegen` fails because no `CONVEX_DEPLOYMENT` is configured.
- BP-09/S1 is preserved as **local commit `d21fa7c` on `codex/bp-09-gateway`** in existing `bp-03-codex`; report: `docs/implementation/reports/BP-09-S1-model-gateway.md`. The controller independently verified 345/345 units, typecheck, lint 0 errors/28 existing warnings, both audits and build. Structured malformed output now records failed, project organization is server derived, and all known direct provider callsites route through the gateway. Full grounded context, monetary budget policy, prompt evals and BP-05 AI-run lifecycle integration remain open; S1 is local/implemented_unverified, not full BP-09 acceptance.
- BP-11 journey-origin fix is local commit `535ae7e` on `codex/bp-11-journey-origin`; BP-12 manual-insight provenance fix is local commit `e57f56e` on `codex/bp-12-manual-insight-truth`. Both have reports under `docs/implementation/reports/` in their commits and are partial slices, not completed chapters. The owner chose EU/EEA multilingual-first analytics/consent rollout with 15 specified languages; see backlog D-13.
- BP-06/S1 billing truth is preserved as **local commit `5c8aba6` on `codex/bp-06-admin-stats`** in existing `review-current`; report: `docs/implementation/reports/BP-06-S1-billing-truth.md`. Owner chose Stripe test catalog and cancellation at period end. Controller verified 354/354 units and targeted lint/audits on the branch; its local `three` dependency is missing, but combined branch `1203d62` passes typecheck/build. No real Stripe test catalog or portal config has been verified. This code overlaps BP-05 billing and must be reconciled later. The three untracked controller docs remain local and preserved.
- BP-11 onboarding source-review slice is local commit `7b5ca43` on `codex/reconcile-reviewed`: mixed website/Google lookup outcomes show `partial`, and a Google Business candidate must be confirmed before its fields are used. The regression was demonstrated red first; the branch passed 389 units/typecheck/targeted lint/build at that point. No provider or browser matching proof. Report: `docs/implementation/reports/BP-11-source-review-onboarding.md`.
- BP-19 social action setup slice is preserved as local commit `eb39e27` on `codex/bp-19-social-action-connection-gate` in `review-current`, cherry-picked as `a69e94d` on reconciliation. Schedule/publish controls wait for configured OAuth credential status, with an explanation. Focused social tests 9/9 and targeted lint passed; `review-current` still lacks `three` for whole-app typecheck. It does not prove live provider validity. Report: `docs/implementation/reports/BP-19-social-action-connection-gate.md`.
- BP-07/S1 ads execution claim is local commit `db64359` on `codex/bp-07-s1-receipt` in `bp-03-codex`, cherry-picked as `1c65224` on reconciliation. Concurrent invocations are fenced by an atomic claim and token-checked receipt; capability is rechecked after refresh before the adapter. Controller verified 10 focused ads/social tests on its branch, typecheck/targeted lint, and 394 combined units/typecheck/audits on `1c65224`. Unknown outcomes remain `executing` pending reconciliation; full BP-07 is open. Report: `docs/implementation/reports/BP-07-S1-ads-execution-claim.md`. The branch adds only ads fields/index to `schema.ts`, which BP-05 also edits.

## Freebuff and release gate

Existing Freebuff BP-09 chat: [old-paths-carry](https://freebuff.com/web/project/old-paths-carry?view=versions). Vly blocks Git/GitHub commands. Its Workspace History showed checkpoint `93a0df2`; six selected file SHA-256 values matched GitHub main, which is **not** full-tree proof. The owner chose Codex to implement and review changes first, then make the reconciled GitHub version available to Freebuff. Freebuff acknowledged a pause before BP-09 edits, autosave restore or publish. Only resume after reviewed integration, exact GitHub SHA and selected-file/checkpoint parity are rechecked.

T0.1/B3 secret remediation remains open. The tracked `.env.keys`/historical credential causes the configured secret scan and overall release gate to fail. Do not print values, weaken scanning, call CI green or deploy. Individual non-security checks can be run and reported exactly. Owner rotation/history remediation is backlog D-01.

## Integration order

1. Preserve BP-05 uncommitted work and all local chapter commits. Fix and independently verify BP-05 export/deletion defects before integrating it. Do not merge a failing safety gate.
2. Integrate each reviewed branch/commit against freshly fetched main with combined tests/CI. BP-03 rollout requires legacy verified-row inventory; BP-09 `aiRuns` registry/schema must reconcile with BP-05 lifecycle; BP-10 and BP-06 branches are separate. Keep each package's status `implemented_unverified` or partial until its acceptance evidence exists.
3. After a reviewed GitHub commit is available and the release remains truthfully classified, give Freebuff its exact SHA and focused file hashes/checkpoint instructions. Keep one new Freebuff chat per next chapter.

## Copy-ready prompt for a new controller task

```text
Continue as the MOSAI controller. First read /Users/julianwitkowski/Downloads/mosai-friday/review-current/docs/implementation/CONTINUITY-HANDOFF.md and its linked backlog, AGENTS.md, blueprint, PROGRESS.md and reports; re-fetch GitHub julekpl/Mosai-friday main before integration. Preserve the complete product vision and truthful provider-status rule. No new worktrees: use the existing BP-02, BP-05, review-current and bp-03-codex checkouts. Existing bp-02-codex checkout holds local reconciliation branch codex/reconcile-reviewed at 1c65224 (BP-02/03, BP-06 admin/S1, partial BP-07/09/10/11/12/19). Controller verified 394 units, typecheck and authorization audits on that exact tip; browser E2E 20 pass/1 opt-in OTP skip was on predecessor 1203d62. BP-05 Codex task codex://threads/01a0cf38-322a-78b0-8e69-e3d50d984537 is fixing independently reviewed export/deletion defects in bp-05-codex. Its prior revision passed 361 units, typecheck and registry audit, but is unsafe to integrate until the current regressions pass. Do not overwrite its uncommitted edits. Other local slice commits: BP-11 7b5ca43, BP-19 eb39e27, BP-07 db64359; see state above.

Freebuff BP-09 chat https://freebuff.com/web/project/old-paths-carry?view=versions is paused and acknowledged; checkpoint 93a0df2 and six selected-file hashes matched old main, but full-tree parity was not proven. The owner chose Codex to reconcile reviewed changes into GitHub before Freebuff resumes. Review code, relevant tests, exact-SHA CI and preview before merging; keep BP-03 legacy-release inventory and BP-05 export/deletion defects visible. BP-06/S1 now displays Stripe test-catalog prices and requests period-end cancellation, but still lacks real test-provider proof. BP-10 source status is not a complete provenance pipeline. BP-12 rollout scope is EU/EEA with 15 owner-specified launch languages in backlog D-13.

The tracked .env.keys/historical credential still fails the secret scan and release gate. Do not print secrets, weaken rules, claim CI green, deploy, or make external writes. Ask only for owner decisions that block real billing/provider/rollout behavior, and keep a copy-ready Freebuff handoff with exact reconciled SHA when one exists.
```
