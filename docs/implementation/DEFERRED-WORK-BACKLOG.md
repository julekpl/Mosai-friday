# MOSAI deferred work and integration backlog

Updated: 24 September 2026. This file tracks cross-chapter debt and owner decisions. `docs/implementation/PROGRESS.md` remains the package-status record; `docs/MOSAI-IMPLEMENTATION-BLUEPRINT.md` and `docs/pack/10-build-backlog.md` define scope and acceptance. A deferred item is not a passed gate.

| ID | Work to finish | Owner / dependency | Exit evidence | Status |
| --- | --- | --- | --- | --- |
| D-01 | T0.1/B3 exposed-secret remediation: rotate/revoke affected credentials, remove tracked `.env.keys`, follow the reviewed history-purge runbook, and rerun both working-tree and full-history scans. Never print values, weaken rules, or allow-list the finding. | Repository owner; `docs/runbooks/secret-rotation.md` | Old credential rejected; both security jobs pass on the exact new default-branch commit; synthetic planted-secret tests still fail as intended. | **Open; release blocked.** Code work may continue with individual non-security checks, but `bun run check`/CI remain red. |
| D-02 | Integrate BP-03 legacy-snapshot compatibility fix from local commit `c1006d3` on `codex/bp-03-final`. Before rollout, inventory persisted verified audits missing route/title snapshots; do not backfill from mutable CMS records or invent provider receipts. Plan fresh verification through BP-13 if affected rows exist. | BP-03 owner + deployment owner; BP-13 verifier for real recovery | Reviewed combined diff, relevant regressions and CI on the integrated SHA; inventory and customer-visible rollout decision recorded. | Open; controller independently reran 21 focused tests; no new-code CI. |
| D-03 | Integrate BP-02 auth work from `codex/bp-02-auth` against current main without overwriting BP-03 or either `PROGRESS.md` edit. A read-only comparison found only `PROGRESS.md` overlap with BP-03 `c1006d3`: both append at the same former EOF and both additions must survive. Select transactional email provider/domain and privileged step-up policy; prove real OTP delivery with an approved test inbox. | Repository owner for O2/step-up; BP-02 developer | Combined auth/publish/non-admin checks and CI; real delivery and privileged-access evidence; report status updated. | Open; BP-02 local implementation is `implemented_unverified`; one stale live-OTP assertion was corrected in its checkout. **24 Sep 2026 verification chat:** the linked Convex deployment's `bun convex env list` (names only) confirms `EMAIL_OTP_API_KEY` is **absent**, so live login on the preview is blocked on O2 configuration, not code. Report: `reports/BP-02-live-auth-verification.md` (includes the post-O2 verification prompt). |
| D-04 | BP-04 provider proof: controlled refresh/reconnect tests for approved social/ad provider accounts. | Owner supplies O7 app approvals/test accounts | Provider receipts and scoped test results; no campaign blast. | Open; mocked implementation `implemented_unverified`. |
| D-05 | Keep reviewed Codex work and Freebuff's Vly checkout aligned on one exact development commit without overwriting shared schema, billing, capability or registry rules. | Controller; owner authorized development sync | Combined diff and tests on the remote commit, CI recorded, Freebuff checkpoint/files checked against it. | Latest reviewed code sync `a594cf75175287f63967264b2b5852cb4e3d792b`, docs `5f73b13`; exact [CI 35977893382](https://github.com/julekpl/Mosai-friday/actions/runs/35977893382) and [CI 35978781895](https://github.com/julekpl/Mosai-friday/actions/runs/35978781895) passed install/typecheck/lint/unit/codegen/e2e/a11y and **failed both secret-scan jobs**. Freebuff matched four CRM-file hashes at `a594cf7`; History includes `5f73b13` plus Vly merge entries. Full-tree parity remains unproven. Release remains blocked. |
| D-06 | Supply or formally retire missing reference documents listed as B1 in `PROGRESS.md`, especially `07-ai-agent-config.md` before BP-09/S3 eval-floor claims. Do not invent their contents. | Repository owner | Files supplied or explicit scope decision recorded. | Open. |
| D-07 | Resolve owner decisions O1–O8 in `PROGRESS.md` before the affected billing, provider, hosting, sandbox, commerce and release claims. | Repository owner with relevant provider/test accounts | Decision record and acceptance proof for the affected package. | Open; unblocked code slices may proceed without fabricated defaults. |
| D-08 | Improve hermetic browser-test startup documentation: a clean local checkout needs an inert syntactically valid `VITE_CONVEX_URL` for the test backend to mount. This must never point to production. | Test-infrastructure chapter | Documented local test command; e2e/a11y pass in a clean checkout without live provider calls. | Open; BP-03 controller run with `http://127.0.0.1:9999` passed 15 browser tests, 1 live OTP test skipped. |
| D-09 | Freebuff's Vly environment rejects read-only Git/GitHub commands and exposes no Git metadata. Workspace History and sampled non-secret file hashes can verify a checkpoint but not the complete tree. The owner chose Codex to implement BP-09. | Codex BP-09 owner + controller | Codex BP-09 reviewed in an existing checkout/branch, combined checks and exact-SHA CI recorded, Freebuff rechecks its resulting checkpoint/files. | BP-09/S1 and bounded S2, including the social draft-variant follow-up, reached GitHub main by `e6f3f10`. Freebuff matched seven sampled files at that checkpoint but has local Vly WIP/merge history. Full-tree parity, BP-09/S3 and provider proof remain open; the old BP-09 Freebuff chat stays paused. |
| D-10 | BP-06 billing truth and catalog: baseline customer page hard-coded €0/€29/€79/€199 and baseline `cancelPlan` changed a local entitlement without Stripe confirmation; return URLs allowed arbitrary HTTP(S). Local S1 commit `5c8aba6` replaces these with server-fetched Stripe test prices, provider-confirmed period-end cancellation, trusted app-origin returns and a safe portal-config gate. Admin still lacks versioned price/package/promotion editing and billing measures; the admin overview's formerly unbounded subscription scan was fixed in local commit `016c087`. | BP-06 owner; **owner chose Stripe test catalog as price source and cancellation at period end** on 23 Sep 2026. Tax presentation, promotions, final package definitions, Stripe test account and portal config proof remain open. | Customer-visible prices match a versioned server catalog and checkout total; cancellation status follows verified provider response/webhook; Stripe return URLs are server-validated; two organizations can hold distinct packages; admin stats have bounded, labeled counts. | Open; S1 and BP-05 are reconciled locally at `7e880f6` (combined 436 units/typecheck/lint/audits/build and browser E2E passed), but no real Stripe test proof or exact-SHA CI exists. |
| D-11 | BP-10 source provenance is only a partial slice: per-source `ok`/`empty`/`needs_setup`/`rate_limited`/`failed` status is returned and shown during the current Create session, but status is not persisted, source facts are not versioned or reviewed as entities, and generation does not trace claims back to facts. Real provider samples, rights and rate limits remain unverified. | BP-10 owner; approved provider keys/test data for external proof | Stored source/run provenance with safe tenant scoping, entity/fact review and generation citations; provider contract and UX checks; no false researched success on all-source failure. | Local commit `c9da779` on `codex/bp-10-research-status`; 336 units passed with `BUN_BIN` set, lint 0 errors; not a completed chapter. |
| D-12 | BP-11 journey evidence and cross-module reuse: project scan fields still lack persisted per-field source review; AI personas have no claim-level evidence/assumption checkpoint; journey stages lack modeled happy paths/outcomes; approved content has no immutable version reference for reuse. The AI-generated-map-as-manual defect was fixed in local commit `535ae7e`. Local slice `7b5ca43` now shows partial website/Google lookup outcomes and requires explicit customer confirmation before a Google listing enters project context. | BP-11 owner; BP-10 persisted facts/evidence and module contracts for full path | A nontechnical end-to-end journey from reviewed source through evidence-linked persona, gap/happy path and approved content reused at exact version; browser proof. | Open; onboarding slice passed 389 units/typecheck/build, but no real provider match or full BP-11 journey has been proven. |
| D-13 | BP-12 measurement: GA4/GSC/Matomo/PostHog/GTM controls are setup placeholders, not verified connectors; no analytics consent collector or reproducible observation store. The user-entered Grow insight provider-impersonation/fabricated-freshness defect is fixed in local commit `e57f56e`. A type-only canonical event/consent contract is now integrated as `6be29f0`/`914346d`; internal commerce events still lack dedupe/consent/attribution and no Grow reader consumes them. | BP-12 owner; **owner chose EU/EEA, English-only first release** on 24 Sep 2026. Other requested languages are deferred until explicitly requested: German, French, Italian, Spanish, Polish, Dutch, Romanian, Greek, Hungarian, Czech, Swedish, Portuguese, Bulgarian, Danish. Server-owned consent record and approved English EU/EEA consent policy, O7 test properties/scopes, and owner-approved GTM configuration remain. | Denied consent makes zero optional tracking requests; provider observations carry account/window/unit/attribution/freshness and reproducible calculations; external test properties verified; English consent copy/journeys work for first release. Later languages need their own translation and browser acceptance before rollout. | Open; no public collector, provider connection or consent rollout. Client-supplied consent was rejected during review; combined unit and capability audit pass with collection absent. |
| D-14 | BP-09 after bounded S2: Build chat, ads copilot and social scheduling advice still lack the ContextPack; client research digests remain unverified request text; claim-level grounding, dollar spend ceilings, run reconciliation and golden evaluations remain. A failed terminal run write can strand a `running` row. | BP-09/S3 and BP-07 owners; missing `07-ai-agent-config.md` blocks measured evaluation-floor claims. | All model paths use authorized, versioned context; outputs cite verified evidence; runs reconcile after crashes; per-agent budget policy and evaluations have approved specifications and measured results. | Open. Bounded ContextPack/inspector and social draft-variant follow-up reached development main `e6f3f10`; foreign-reference and unrequested-platform regressions pass. Combined 464 units and synthetic browser 20+1 passed; exact CI passed non-security jobs and failed both secret scans. Live model proof remains open. |
| D-15 | BP-05 export/deletion lifecycle safety and customer journey: resumable jobs, shared ownership, late subscription checks, active-member authorization, bounded export and deletion, current-access chunk reads; customer export/download UI and deployment proof remain. | BP-05 owner/controller; no live deletion. | Local regressions plus combined integration checks, then dev Convex/provider proof and usable self-service export/download flow. | **Safety regressions fixed and locally integrated** as source `0fde75c`, combined `7e880f6`. Independent Luna review passed 39 focused tests; combined 436 units and all 66 registry entries pass. Still `implemented_unverified`: no real Stripe/Convex proof, export UI, exact-SHA CI or green release gate. |
| D-16 | BP-07 execution protocol remains partial. Local S1 commit `db64359` adds a one-change atomic claim and token-checked ads receipt with a post-refresh capability recheck; abandoned or ambiguous operations remain `executing`. It does not implement provider idempotency propagation, lease recovery, reconciliation/replay, transactional events, payload-bound approvals or full job UI. | BP-07 owner; provider-specific idempotency contracts and operator reconciliation design | Concurrent executions at most once; timeout-after-provider-success reconciles without duplicate writes; stale approval and capability removal prevent spend; dead letters/replay use same logical ID; exact provider test receipts. | Open; S1 cherry-picked as `1c65224`, 394 combined units/typecheck/audits passed, no provider proof or full BP-07 acceptance. |
| D-17 | BP-19 remains a product journey, not merely navigation: the mobile drawer restores module/account access and the new Overview guide suggests one next action from saved work and access state. Neither feature proves source review, full guided journeys or observed usability outcomes. A stored social credential is not provider verification; accepted/published receipts still need BP-18. | BP-19/BP-18 owners; BP-08 account verification; owner-coordinated user sessions | Authenticated mobile drawer and Overview visually/interaction checked; five nontechnical users complete key journeys at blueprint threshold with zero critical live/paid misreadings; provider receipts before social operation claims. | Open. Mobile navigation and Overview next-action follow-up reached development main `e6f3f10`; six-screen public preview audit is recorded. Combined 464 units and synthetic browser 20+1 pass, but no authenticated browser or five-user proof. Full BP-19 acceptance remains open. |
| D-18 | BP-07/BP-05 race: project or account deletion could cascade an `executing` ads change while its provider action was in flight, deleting the claim before the receipt mutation. AI-run telemetry has a related lower-severity in-flight loss path. | BP-07/BP-05 integration owner; provider-specific recovery policy remains open. | Red-first interleaving regression; atomic claim/deletion fence; manual reconciliation for ambiguous outcomes; provider proof before operational completion. | **Ads race fixed locally, operational recovery open.** Source `3e9ce53`, integrated `7b120dc`; 5 new regressions and 442 combined units passed after measurement audit correction. Deletion now waits for a receipt; an indefinitely executing claim blocks deletion for manual reconciliation. No real provider or exact-SHA CI proof. AI-run path remains open. |
| D-19 | BP-08 beyond OAuth state/origin: bind state to organization and explicit redirect target, approve token vault/encryption, support account discovery/selection, verify scopes and real sync freshness, and stop jobs on disconnect. | BP-08 owner; O7 provider apps/test accounts and token policy decision | Controlled wrong-tenant/replay, account choice, revoked credential, first sync and disconnect journeys with provider receipts; no tokens in client or model context. | Open. S1 reached development main `f5a68aa`; it closes the query/delete replay race and request-host redirect fallback. Exact CI passed non-security jobs and failed both secret scans. No real OAuth/provider proof or full BP-08 acceptance. |
| D-20 | BP-15 app requirements now have a separate workspace, but there is no app-specific source graph, build job, runner, isolated preview, export or deployment. A saved/reviewed brief must never be described as a working app. | BP-15/S1 owner; O5 sandbox/backend host is needed before S2 deploy claims, not before remaining interface work. Owner chose customer-facing and internal team apps equally; audience is explicit. | Authenticated app CRUD and requirements review journey, source-change and tenant tests, then source graph/job/runner contract; later sandbox/preview/deploy isolation proof requires O5. | Open. BP-15/S1a reached development main `5f89ed1` with 7 focused regressions and an app-specific brief with server provenance/review; report `docs/implementation/reports/BP-15-S1a-app-requirements.md`. Exact CI passed non-security jobs and failed both secret scans; browser suite only covers public/auth, not this authenticated app path. |
| D-21 | BP-17/S1a imports only canonical new email addresses and removes unsupported consent grants. Finish append-only purpose/channel consent evidence, withdrawal and suppression, contact-level erasure, and a bounded migration/index for legacy mixed-case addresses; do not reinterpret existing booleans as verified opt-in. | BP-17/S1 owner; owner-approved consent policy for the eventual grant flow. O2 email provider/domain is needed for S2 sending proof. | Wrong-tenant and duplicate import regressions, explicit consent provenance and withdrawal checks, legacy dedupe migration/rollback evidence, suppression before sends, and customer-visible journey proof. | Open; S1a reached development main `a594cf7`; exact [CI 35977893382](https://github.com/julekpl/Mosai-friday/actions/runs/35977893382) passed non-security jobs and failed both secret scans. No send path or verified consent ledger exists. |
| D-22 | BP-13/S1 internal public-host projection is local and uncommitted. Its new Convex module path needs official generated bindings before development sync; local `convex codegen --dry-run` exits because `CONVEX_DEPLOYMENT` is unset. No hostname verifier writes the receipt field, and no separate-origin HTML, domain ownership, TLS or live public page is proven. | BP-13 owner; configured **development** Convex deployment for codegen; owner O4 public hosting/domain decision for S2 and real origin proof. | Official codegen produces committed bindings with exact-SHA drift check green; verified host receipt + immutable release projection, separate-origin JS-disabled HTML/browser checks and provider/domain proof. | Open; source and 22 focused tests are in `bp-02-codex`, report `docs/implementation/reports/BP-13-S1-public-projection.md`. Keep it out of GitHub main until codegen and review; do not hand-edit `_generated` or claim BP-13/S1 complete. |
| D-23 | BP-16 OpenAI Ads requires a real account-scoped API-key onboarding and vault model, account verification, contract-aligned paused creation/insights, exact approval and provider receipts. The prior guessed OAuth/endpoints must stay disabled. | BP-16 owner; approved OpenAI Ads test account/key and credential handling decision (O7). | Controlled key setup and `GET /v1/ad_account` proof; paused campaign/ad-group/ad creation with idempotency and review status; approved activation and metrics with accurate currency/window, revocation and wrong-tenant tests. | Open. [Official Advertiser API overview](https://developers.openai.com/ads/api-overview) and [authentication](https://developers.openai.com/ads/api-reference/authentication) are published. BP-16/S0 removes the false operational path with 5 focused regressions in development main `5f89ed1`; see `docs/implementation/reports/BP-16-S0-openai-ads-truth.md`. No credential, provider call, or live-operation proof exists. |
| D-24 | BP-14/S1 needs merchant onboarding/readiness, signed-out checkout and real payment/order/fulfillment, public product feed acceptance diagnostics, and per-store Shopify proof. Local `active` catalog status is only local storefront visibility. | BP-14 owner; O6 Stripe Connect/tax/shipping/refund decision before money movement; O4 public origin and O7 test merchant/store accounts for external proof. | Wrong-tenant catalog checks, test-mode checkout and verified webhook, no oversell, refund/fulfillment reconciliation, external feed outcome, and Shopify credential/webhook isolation. | Open. Bounded catalog truth slice reached development main `5f89ed1`; validates collection ownership, integer minor-unit amounts and inventory, variant readiness and native sync semantics; report `docs/implementation/reports/BP-14-S1-catalog-truth.md`. No transaction, provider or authenticated browser proof. |

| D-25 | Close the remaining user-reported MVP gaps: verify live research-provider fetches and website/Google Business enrichment; add source/content attachments to persona and analyst review; support batch persona creation and evidence-backed country context; complete authenticated admin account normalization/navigation; turn Sell into an actual customer storefront and Build into a useful website/app editor; redesign the relationship map; prove campaign draft generation, content generation and project deletion in-browser. Competitor lookup must distinguish URLs from business profiles and show the selected match before saving. | MOSAI product owner; provider credentials/test accounts and Freebuff preview access for live evidence. | Authenticated responsive journeys with precise empty/loading/error/partial/success states; provider-specific findings and failure evidence; source citations and persona/journey context visible in generated copy; tenant/ownership tests; external preview proof. | Open. Current local slice improves the content-to-editor handoff, source error categories, persona fields, connection timeout visibility, new-project inputs, journey gap navigation, module descriptions and owner-only project deletion. Static checks and focused unit tests pass; no live-provider or browser proof, full test suite and release gate remain blocked. |

## Working rule while D-01 is open

For D-13, English is the only first-release language. The 14 others are a deferred roadmap list, not a launch requirement or localization claim. A read-only scan on 24 September found no application locale/translation files or translation hook in `src`; current UI copy is English. Do not spend the first-release critical path on untranslated locale infrastructure. Later language phases need a shared locale strategy, approved translations and language-specific browser checks when the owner requests them.

Run and report typecheck, lint, unit, authorization audits, browser checks and codegen separately where their prerequisites exist. Run the configured `bun run check` and record its actual failure at `scan:secrets`; never edit the scan or represent the overall gate as passed. No production deployment, live external write, or release approval follows from passing the other checks.

## Parallel chapter boundary

BP-05 source is preserved as `0fde75c`, integrated at `7e880f6`. BP-12's type-only measurement contract is preserved as source `de219e0` in `bp-03-codex`, integrated `6be29f0` and corrected at `914346d` after the combined capability gate rejected a public collector in an internal file. The BP-07/BP-05 deletion fence is preserved as source `3e9ce53` in `bp-05-codex`, integrated `7b120dc`. The reviewed code sync containing BP-08/S1, BP-09/S2 and BP-19 navigation/next-action follow-ups reached GitHub main at `e6f3f104862565c1632cd13c563e23cdcc6f8616` on 24 September; exact CI passed non-security jobs and failed both secret scans. Use `git rev-parse HEAD` and GitHub CI for the latest authoritative checkpoint. Earlier sources remain preserved: BP-02 `9a12f03`, BP-03 `c1006d3`, BP-06 admin `016c087`, BP-10 source status `c9da779`, BP-11 origin `535ae7e`, BP-12 manual insight `e57f56e`, BP-09/S1 `d21fa7c`, BP-06/S1 `5c8aba6`, BP-11 onboarding `7b5ca43`, BP-19 social gate `eb39e27`, and BP-07/S1 ads claim `db64359`. No new worktrees are authorized. Freebuff matched seven sampled files at `e6f3f10`; its old BP-09 chat remains paused. Future Freebuff chapters need a new chat and exact current checkpoint.
