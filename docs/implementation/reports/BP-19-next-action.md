# BP-19 follow-up — project Overview next action

**Status: implemented_unverified (bounded first-run guidance).** Overview now places one recommended next action above its detail sections and shows a small setup checklist. It uses the existing authorized persona, journey and content queries plus the server-resolved included-module list. The model distinguishes no saved work, saved work needing review and locked access; it does not call a row complete, verified or published. The action links to the actual module or plan options. Loading and query-error states use the existing skeleton and error boundary. The component uses semantic headings/list/link and responsive layout.

Changed paths: `src/pages/app/Overview.tsx`, `src/components/app/NextAction.tsx`, `src/components/app/next-action-model.ts`, `tests/unit/overview-next-action.test.ts`. The focused model suite passes 5/5, including a locked module with saved rows. Combined integration passed 464/464 units, typecheck, lint (0 errors/28 existing warnings), build and all three audits; the hermetic Playwright suite passed 20 with one live-OTP skip. Exact code commit `e6f3f10` [CI 35974027409](https://github.com/julekpl/Mosai-friday/actions/runs/35974027409) passed non-security jobs and failed both secret scans. `bun run check` still exits 1 at the existing tracked secret finding. The browser-visible public first-run audit and six screenshots are in `docs/implementation/ux-audit-2026-09-24/`.

**Limit:** The current persona/journey/content records lack a durable reviewed/approved prerequisite signal usable by this guide. Row presence can suggest a next draft activity but cannot certify review; copy calls this out and the checklist says “saved,” not “complete.” This is not the full BP-19 journey. Authenticated mobile/desktop interaction, exact source-review linkage, all module states and the required five observed nontechnical user sessions remain open. The public Freebuff preview was tested only through the fictional walkthrough and sign-in entry; no real OTP or first-project outcome was proven.
