# BP-19 — mobile module and account navigation slice

**Status: implemented_unverified (bounded slice).** Developed from GitHub main `274877f` in the existing `bp-02-codex` checkout and locally combined with BP-08/S1 and BP-09/S2. BP-19's overall guided journey and observed-user acceptance remain open.

Below the desktop breakpoint, the app now offers a compact Radix Sheet navigation drawer. It exposes project switching/new project, all eight entitlement-aware modules, billing, authorized platform administration and sign-out. It names the current location, keeps locked/setup states visible, supports keyboard-managed dialog focus and uses 44px minimum targets. This corrects a static audit finding that the previous mobile header only returned to overview after the full sidebar disappeared.

Changed paths: `src/components/app/AppShell.tsx` and `tests/unit/app-shell-mobile-navigation.test.ts`. The focused SSR regression failed against the previous AppShell and passes now; it covers the module/account menu, current-route announcement and conditional admin visibility. Combined local checks passed 456/456 units, typecheck, build, lint with zero errors/28 existing warnings, all three audits, and the synthetic browser suite 20/20 with one intentional live-OTP skip. The browser suite covers public/auth journeys, **not** an authenticated mobile drawer. Its signed-out fixture cannot reach that UI; authenticated visual and interaction verification remain open. A first local E2E attempt rendered blank because `VITE_CONVEX_URL` was absent; rerunning with the documented synthetic CI URL passed. The release gate still fails the known secret scan.

**Still open:** A task-oriented first-run checklist and next action, evidence review across ingestion/persona/journey/content, honest incomplete-module messaging, responsive visual checks of the authenticated drawer, and BP-19's five observed nontechnical-user sessions with the required success and truthfulness outcomes. No module, payment, provider or publication was made operational by this navigation change.
