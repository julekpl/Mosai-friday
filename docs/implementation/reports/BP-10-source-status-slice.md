# BP-10 source-status slice — 23 September 2026

Status: **partial implementation; BP-10 is not complete**. Base: GitHub main `93a0df20ce5bf9568bcf5a2ebbed6080894a32f4`. Branch: `codex/bp-10-research-status` in the existing `review-current` checkout.

`research.researchTopic` now returns per-source result envelopes instead of turning every source failure into an empty result. Each envelope distinguishes `ok`, `empty`, `needs_setup`, `rate_limited`, and `failed`, carries a checked time and safe error category, and never returns provider error text. The unsupported YouTube HTML/transcript scrape was removed; YouTube search now requires the existing SerpAPI configuration. The Create topic view gives a concise, expandable source summary. If no findings are returned because a source is unavailable, it leaves existing topic findings and status untouched and explains what happened. A successful zero-result search reports no matches rather than success with zero findings.

Verification: focused source-provenance tests pass (3/3); full unit suite passes (336/336) with `BUN_BIN` set to the available Bun 1.3.14 binary; lint passes with 0 errors and 28 existing warnings; authorization audit passes (221 scanned, 3 existing review notices); capability audit passes (152 scanned); `git diff --check` passes. Typecheck remains blocked by the pre-existing incomplete `node_modules` installation: seven `TS2307` errors for the declared `three` dependency in untouched `RelationMap.tsx`. The first unit run without `BUN_BIN` failed six audit-gate subprocess tests because `bun` was absent from PATH; rerun with `BUN_BIN` passed. No live provider call, preview journey, full release gate, or secret-scan remediation was performed for this slice.

Remaining BP-10 work: persist source/run provenance and fact versions with tenant scope; review extracted entities/facts; trace generated claims to reviewed evidence; verify provider payloads, rights and rate-limit behavior with approved test accounts; complete the blueprint acceptance journeys. Source statuses currently live only in the Create component's session state and disappear on reload. This slice cannot be represented as an end-to-end data ingestion or content-grounding solution.
