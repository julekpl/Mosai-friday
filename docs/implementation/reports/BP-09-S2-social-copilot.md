# BP-09/S2 follow-up — social copilot context boundary

**Status: implemented_unverified (bounded draft-variant path).** The social `draftVariants` action now loads the authorized server ContextPack after project access is checked. It refuses foreign persona, campaign and content IDs before calling the model. The internal draft mutation rechecks campaign/content project ownership before inserting a post. The request's source text is visibly labeled untrusted in the prompt, and only complete surviving evidence references enter gateway provenance. Model output must contain exactly the requested set of platforms; unrequested or duplicate platforms cannot create drafts. All outputs remain drafts, not posts sent to providers.

Changed paths: `src/convex/social/copilot.ts`, `src/convex/social/copilotData.ts`, `tests/unit/context-pack.test.ts`. The agent's initial social test covered foreign personas and prompt labeling. Controller review found campaign/content cross-project reads and references, plus a platform output validator that accepted an extra valid platform. A new platform regression failed before the validator fix: a LinkedIn-only request created both LinkedIn and TikTok drafts. The corrected focused suite passed 8/8. Combined integration passed 464/464 units, typecheck, lint (0 errors/28 existing warnings), build and all three audits; the hermetic Playwright suite passed 20 with one live-OTP skip. Exact code commit `e6f3f10` [CI 35974027409](https://github.com/julekpl/Mosai-friday/actions/runs/35974027409) passed non-security jobs and failed both secret scans. `bun run check` still exits 1 at the existing tracked secret finding. Authenticated social UI remains unverified.

**Still open:** `suggestSchedule` uses user-authored body with generic platform advice, not analytics evidence; the ads copilot and Build chat still lack this ContextPack. Source text is not checked against a saved approved content revision, so no approved-source claim is made. Provider delivery, platform-specific text/media limits, approvals and receipts belong to BP-18. Live model/provider and authenticated browser behavior remain unverified. The release gate remains blocked by the tracked secret finding.
