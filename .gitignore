# ── Secrets ──────────────────────────────────────────────────────────────
# The dotenvx private key (.env.keys) and a live OTP email API key were
# committed to this repository while it was public and must be treated as
# compromised. Only the documented example file may ever be committed.
# See docs/runbooks/secret-rotation.md (MOSAI pack T0.1 / G19).
.env*
!.env.example

# ── Build and tooling ────────────────────────────────────────────────────
node_modules
dist

# src/convex/_generated is COMMITTED (ticket T1.2, decided 22 Sep 2026).
# Convex's own guidance is to check the bindings in so the project typechecks
# and runs without first running `convex dev`. Do not add it back here; the
# `bun run check:codegen` drift check keeps it honest.
